#サプーのYoutube動画「【超簡単Webアプリ】streamlitでWebアプリを最速で作ってネット公開！〜 プログラミング初心者向け 〜」
https://youtu.be/4nsTce1Oce8?si=o_aC7iZ6vrWB7PBn&t=1716

git config --global user.name "kojicm26"

git config --global user.email "ko.ji.1101050@gmail.com"

git config --global --list

git init


#gitにデプロイする流れ　add　→　commit　→　push


git add .
git add ./requirements.txt
git add ./main_app.py

git status

git commit -m "first commit"
git commit -m "20231218-12"
git commit -m "osaka20231219-02"

#コミット不要なファイルのリスト用ファイル
git add .gitignore

git remote add origin https://github.com/kojicm26/SanOsaka6_MAP_app.git

git push -u origin main

# gitのremote URLを確認する
git remote -v

.gitignoreの作成
echo "test2.txt" >> .gitignore # サンプル
echo "Anaconda Powershell Prompt.lnk" >> .gitignore
echo "gitコマンド他.txt" >> .gitignore
echo "streamlit run webapp.py.txt" >> .gitignore
echo ".ipynb_checkpoints" >> .gitignore
echo "git\343\202\263\343\203\236\343\203\263\343\203\211\344\273\226.txt" >> .gitignore


ローカルに残しつつgit管理から除外
git rm --cached gitコマンド他.txt
git rm --cached pages/streamlit_app.ipynb
git rm --cached Anaconda Powershell Prompt.lnk # 「 」スペースはgitでは認識しない？
git rm --cached -r .ipynb_checkpoints # フォルダ


削除したいフォルダ・ファイルをリポジトリから削除する
git rm --cached -r 削除したいディレクトリ
git rm --cached ファイル名


#GITのリポジトリ情報ページ
https://github.com/kojicm26/SanOsaka6_MAP_app

#GITのリポジトリ「SanOsaka6_MAP_app」のulr
https://github.com/kojicm26/SanOsaka6_MAP_app.git





$ sudo chown -R username:groupname /var/www/html/sample_homepage

$ sudo chown -R kojicm26 /var/www/html/sample_homepage

# https://github.com/streamlit/gsheets-connection/blob/main/setup.py
# https://pypi.org/project/st-gsheets-connection/
